320121197809151713 952789
http://www.95598.cn/
zanjun9527	z3-4

外网和内网使用
分库分表的设计

solradmin中的df查询

maven-jar-plugin 和maven-dependency-plugin在main启动dubbo时的使用

主从数据库读写分离

netty的基本了解

双master的消息写入策略，是分别写还是写入同样的


synchronized和lock的区别

多线程的对象的公用和私有

VisualSVN  搭建svn服务器，copssh安装gi私服 maven的仓库管理软件——Sonatype Nexus搭建私服 有空再搭建

JeeSite项目可以看看

多个微服务的dubbo的架构问题，

@EnableConfigurationProperties 再看看
httpclient 的请求和接收
idea target地下不自动更新
自定义相同的拦截路径，后面就不进mvc了，配置成/**进拦截器，但是controller没走print方法，对于mv没有返回的会执行三次拦截器，return  就好了
@WebServlet(
        name="myServlet",
        urlPatterns="/login/*",
        loadOnStartup=1 )


   private FilterConfig config;

    @Override
    public void init(FilterConfig config) throws ServletException {
        this.config = config;
    }
<<<<<<< .mine
  filter的启动初始化参数

=======
  filter的启动初始化参数

>>>>>>> .theirs
动态代理  service 注入的是 代理类还是service bean  再aop代理的过程中

spring-boot 热加载
boot中使用mapperscan 代替代码配置应该也行吧，需要搭配@mapper标签。其中@select 和@results搭配使用
